@extends('layouts.app')
@section('title', 'Verifikasi Portofolio')
@section('content')
<div class="container-fluid"><div class="page-header mb-4"><div><h2 class="fw-bold mb-1">Verifikasi Portofolio</h2><p class="text-muted mb-0">Tinjau dan sahkan data prestasi serta sertifikat siswa.</p></div></div>
<div class="glass-card p-4 mb-4"><h5 class="fw-bold mb-3"><i class="bi bi-trophy-fill text-warning me-2"></i>Prestasi Siswa</h5><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Siswa</th><th>Prestasi</th><th>Level</th><th>Tanggal</th><th>Status</th><th class="text-end">Aksi</th></tr></thead><tbody>@forelse($achievements as $item)<tr><td><strong>{{ $item->student->name ?? $item->student->user->name }}</strong><br><small class="text-muted">{{ $item->student->nis }}</small></td><td><strong>{{ $item->title }}</strong><br><small class="text-muted">{{ $item->organizer }}</small></td><td>{{ $item->level }}</td><td>{{ $item->date->format('d M Y') }}</td><td><span class="badge {{ $item->status === 'verified' ? 'text-bg-success' : ($item->status === 'rejected' ? 'text-bg-danger' : 'text-bg-warning') }}">{{ $item->status === 'pending' ? 'Menunggu' : ucfirst($item->status) }}</span></td><td class="text-end">@if($item->status === 'pending')<form class="d-inline" method="POST" action="{{ route('teacher.verify', ['type'=>'achievement','id'=>$item->id]) }}">@csrf @method('PATCH')<input type="hidden" name="status" value="verified"><button class="btn btn-sm btn-success">Terima</button></form><form class="d-inline" method="POST" action="{{ route('teacher.verify', ['type'=>'achievement','id'=>$item->id]) }}">@csrf @method('PATCH')<input type="hidden" name="status" value="rejected"><button class="btn btn-sm btn-outline-danger">Tolak</button></form>@else <span class="text-muted small">Sudah diproses</span>@endif</td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-4">Belum ada data prestasi.</td></tr>@endforelse</tbody></table></div></div>
<div class="glass-card p-4"><h5 class="fw-bold mb-3"><i class="bi bi-patch-check-fill text-primary me-2"></i>Sertifikat Siswa</h5><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Siswa</th><th>Sertifikat</th><th>Penerbit</th><th>Tanggal</th><th>Status</th><th class="text-end">Aksi</th></tr></thead><tbody>@forelse($certificates as $item)<tr><td><strong>{{ $item->student->name ?? $item->student->user->name }}</strong><br><small class="text-muted">{{ $item->student->nis }}</small></td><td>{{ $item->title }}</td><td>{{ $item->issuer }}</td><td>{{ $item->issued_at->format('d M Y') }}</td><td><span class="badge {{ $item->status === 'verified' ? 'text-bg-success' : ($item->status === 'rejected' ? 'text-bg-danger' : 'text-bg-warning') }}">{{ $item->status === 'pending' ? 'Menunggu' : ucfirst($item->status) }}</span></td><td class="text-end">@if($item->status === 'pending')<form class="d-inline" method="POST" action="{{ route('teacher.verify', ['type'=>'certificate','id'=>$item->id]) }}">@csrf @method('PATCH')<input type="hidden" name="status" value="verified"><button class="btn btn-sm btn-success">Terima</button></form><form class="d-inline" method="POST" action="{{ route('teacher.verify', ['type'=>'certificate','id'=>$item->id]) }}">@csrf @method('PATCH')<input type="hidden" name="status" value="rejected"><button class="btn btn-sm btn-outline-danger">Tolak</button></form>@else <span class="text-muted small">Sudah diproses</span>@endif</td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-4">Belum ada data sertifikat.</td></tr>@endforelse</tbody></table></div></div></div>
@endsection
